统一规则文档说明： 
1.为确保智能合约、前端展示与测试逻辑保持一致，InnovateDAO 采用统一规则口径如下： Proposal 的状态统一定义为 Pending、Active、Succeeded、Failed、Executed，但在当前实现中，createProposal 成功后即进入 Active 状态并开始计时，而不再停留在 Pending；
投票权按用户质押量计算，但单个地址的有效投票权上限为总质押量的 33%；quorum 统一定义为参与投票总量达到 totalStakedSupply 的 60%，supermajority 统一定义为赞成票占已投票总量 超过 66.6%；Proposal 创建时需锁定一笔 refundable deposit，当前统一规则为：提案成功执行后退还 deposit，未达门槛或执行失败则 proposal 记为 Failed，deposit 默认不退还，以体现 anti-spam 约束；executeProposal 只能在投票截止后调用，且当前实现口径为任何人都可以执行，只要 quorum 和 supermajority 均满足；用户一旦投票，其 stake 会被锁定，需在 proposal 截止后通过 unlockVote() 解锁，且只有在 activeVotes == 0 时才允许 withdrawStake()；前端页面需统一展示的钱包与治理信息包括：用户是否持有 membership NFT、当前 staked balance、当前 voting weight、proposal id、proposer、recipient、amount、description、start time、deadline、support/against tally、current status，以及用户是否已投票、是否可执行等关键字段。以上口径用于统一合约开发、前端联调、测试验证和最终 demo 展示，避免出现规则解释不一致的问题。

2.后续同步应该围绕用户流程：
1.	connect wallet
2.	stake
3.	create proposal
4.	vote
5.	execute
6.	withdraw

3.同步确认改过的地方

分工：
	盛：规则设计与智能合约负责人
负责项目链上核心规则的设计与统一口径维护
负责 Solidity 智能合约整体架构设计与实现
负责 Proposal、Voting、Execution、Staking、Withdraw 等核心模块开发
负责 NFT membership access control、33% voting cap、60% quorum、66.6% supermajority、vote locking 等治理规则实现
负责输出合约接口说明、状态流转说明和关键 event 说明，确保前端与测试理解一致
负责修复合约逻辑问题，并配合联调和部署优化实现
	李：前端开发与演示流程负责人
负责 DApp 前端页面设计、交互实现与整体用户体验
负责钱包连接、合约调用、交易发起与结果反馈
负责提案展示、创建提案、投票、执行提案等核心页面开发
负责前端与智能合约联调，确保主要用户流程可以跑通
负责设计和整理最终 demo 流程，确保展示逻辑清晰、操作顺畅
负责前端报错提示、状态展示和 demo 期间的页面稳定性
	田：测试、验收与文档整合负责人
负责测试方案设计与测试用例编写
负责使用 Hardhat / Foundry 对核心功能和边界情况进行验证
重点测试 failed quorum、double voting、withdrawal restriction、execution failure、vote locking 等关键场景
负责记录 bug、跟进修复结果并进行回归测试
负责本地 / 测试网部署检查、演示环境准备和 backup demo material 整理
负责整理 README、Technical Brief、Architecture Diagram、testing summary、AI usage documentation 等最终文档
	协作机制
所有治理规则先统一确认，再进入开发，避免前后逻辑不一致
每个核心模块都设定“主负责人 + 验收人”，确保开发结果可检查、可追踪
每次同步以“核心流程是否能跑通”为准，而不是只汇报各自写了什么
所有重要修改需同步记录，确保信息透明，避免成员之间理解不一致
	共同任务
共同讨论项目需求、MVP 范围和治理规则
共同确认核心用户流程和最终展示重点
共同完成 testnet / localhost demo 准备
共同制作 final presentation slides
共同参与 demo rehearsal 和答辩准备
